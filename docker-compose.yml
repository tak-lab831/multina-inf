services:
  app:
    build: ./app
    expose:
      - "8000"

  proxy:
    image: nginx:latest
    ports:
      - "80:80"
    volumes:
      - ./proxy/nginx/nginx.conf:/etc/nginx/conf.d/default.conf
    depends_on:
      - app
